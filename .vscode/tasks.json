{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run WSL with Venv",
            "type": "shell",
            "command": "bash -c 'cd ${workspaceFolder} && source ${workspaceFolder}/../venv/bin/activate && exec bash'",
            "problemMatcher": [],
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "panel": "shared"
            }
        }
    ]
}